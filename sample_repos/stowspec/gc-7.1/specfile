;;-*- mode: scheme; syntax: scheme -*-;;
(define-stowspec '(
 (conf . "if [ ! -e ./configure ] ; then
  echo 'Error: stowspec supports ./configure script included tar ball only, sorry...'
  exit 1
fi
./configure --prefix=/usr/local/stow/gc-7.1 
")
 (make . "make")
 (install . "make install")
 (arc . "gc-7.1.tar.gz")
 (url)
 (projname . "gc-7.1")
))
